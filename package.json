{
  "name": "tiktok-metadata-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready Telegram bot that analyzes TikTok video URLs and returns technical + TikTok metadata. Runs on Vercel; heavy ffprobe/ffmpeg work is delegated to a separate worker service.",
  "type": "module",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "set-webhook": "node scripts/set-webhook.js",
    "delete-webhook": "node scripts/delete-webhook.js",
    "dev": "vercel dev"
  },
  "dependencies": {
    "@vercel/functions": "^1.5.0",
    "pg": "^8.12.0"
  }
}
